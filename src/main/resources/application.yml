spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  datasource:
    url: jdbc:mysql://fitnessu-db.cqgux8nf2jkp.ap-northeast-2.rds.amazonaws.com:3306/fitnessu-DB
    username: admin
    password: tjrwlswntkdwnsrb1!

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        #      show_sql: true
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace